# cosmosdb-expert

This Claude Skill provides specialized guidance for: Use when working on Azure Cosmos DB capacity, RU consumption, storage pressure, indexing, partition keys, queries, Mongo API behavior, compaction, backup, availability, or performance troubleshooting.

Install it by copying this folder into `~/.claude/skills/cosmosdb-expert` or by running the repository installer.
